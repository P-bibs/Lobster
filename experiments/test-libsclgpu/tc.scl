type edge(x: u32, y: u32)
type path(x: u32, y: u32)

rel edge(1,2)
rel edge(2,3)
rel edge(3,4)
rel edge(4,5)
rel edge(5,6)
rel edge(6,7)

rel path(x, y) = edge(x, y)
rel path(x, y) = edge(x, z), path(z, y)
